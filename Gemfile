source 'https://rubygems.org'

group :jekyll_plugins do
    # Only include wdm on Windows platforms
    gem 'wdm', '~> 0.1.0', :platforms => [:mingw, :mswin, :x64_mingw]
    
    gem 'classifier-reborn'
    gem 'jekyll'
    gem 'jekyll-archives'
    gem 'jekyll-email-protect'
    gem 'jekyll-feed'
    gem 'jekyll-get-json'
    gem 'jekyll-imagemagick'
    gem 'jekyll-jupyter-notebook'
    gem 'jekyll-link-attributes'
    gem 'jekyll-minifier'
    gem 'jekyll-paginate-v2'
    gem 'jekyll-scholar'
    gem 'jekyll-sitemap'
    gem 'jekyll-toc'
    gem 'jekyll-twitter-plugin'
    gem 'jemoji'
    #gem 'mini_racer'
    gem 'unicode_utils'
    gem 'webrick'
end

group :other_plugins do
    gem 'feedjira'
    gem 'httparty'
end

# Use 'listen' for file change detection on non-Windows platforms
gem 'listen', '~> 3.3', :platforms => [:ruby, :jruby]

# Windows does not include zoneinfo files, so bundle the tzinfo-data gem for Windows platforms
gem 'tzinfo-data', platforms: [:mingw, :mswin, :x64_mingw, :jruby]
